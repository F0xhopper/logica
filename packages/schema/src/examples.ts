/**
 * The fixed sample loaded by "or try an example" (DESIGN.md §3). The API's mock pipeline
 * returns a hand-built map for exactly this text, following the worked example in PIPELINE.md §11.
 */
export const EXAMPLE_ESSAY = `We should ban phones in schools. Studies show students who use phones in class get lower grades. Besides, every teacher I've spoken to wants them gone.

Some argue phones are needed for emergencies, but schools have landlines.`;
